pipeline {
    agent any

    

    stages {
       stage ('TF Initialize') {
            steps {
                sh "terraform init -input=false"
            }
        }

        
       stage ('TF destroy') {
            steps {
                sh "terraform destroy -auto-approve"
            }
        }
        
     }

        
}

       
